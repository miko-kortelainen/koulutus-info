import type { Config } from "vike/types";
import { DEFAULT_CUTOFF_YEAR } from "@/config/cutoffRounds";

export default {
  title: `Todistusvalintalaskuri ${DEFAULT_CUTOFF_YEAR} – Laske todistuspisteesi`,
} satisfies Config;
