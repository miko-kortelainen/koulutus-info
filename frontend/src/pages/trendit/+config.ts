import type { Config } from "vike/types";
import { DEFAULT_CUTOFF_YEAR } from "@/config/cutoffRounds";

export default {
  title: `Korkeakoulujen yhteishaun haetuimmat alat ja koulut – ${DEFAULT_CUTOFF_YEAR}`,
} satisfies Config;
