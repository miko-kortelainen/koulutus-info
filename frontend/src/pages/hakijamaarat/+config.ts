import type { Config } from "vike/types";
import { DEFAULT_CUTOFF_YEAR } from "@/config/cutoffRounds";

export default {
  title: `Korkeakoulujen yhteishaun hakijamäärät ${DEFAULT_CUTOFF_YEAR}`,
} satisfies Config;
